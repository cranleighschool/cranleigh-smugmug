jQuery(function($) {
	$(document).ready(function(){
		$(document).on('click', '.smugmug_insert', function () {
            tb_show("Insert Smug Mug", "#TB_inline?inlineId=insert_smugmug", "");
        });
	});
});